#include "Configuration.h"

const char* config::VERSION_STRING = "0.1";

const int config::HEADER_FONT_SZ = 10;

const double config::MIN_TRIANGLE_SZ = 0.05;
const double config::MAX_TRIANGLE_SZ = 50.0;
const double config::DEFAULT_TRIANGLE_SZ = 0.5;

const double config::MIN_WALLS_HEIGHT = 0.05;
const double config::MAX_WALLS_HEIGHT = 500.0;
const double config::DEFAULT_WALLS_HEIGHT = 2.0;

const double config::VIEWPORT_MIN_X = -500000.00;
const double config::VIEWPORT_MAX_X = 500000.00;
const double config::VIEWPORT_MIN_Y = -500000.00;
const double config::VIEWPORT_MAX_Y = 500000.00;
const double config::DEFAULT_VIEWPORT_MIN_X = -5.0;
const double config::DEFAULT_VIEWPORT_MAX_X = 5.0;
const double config::DEFAULT_VIEWPORT_MIN_Y = -5.0;
const double config::DEFAULT_VIEWPORT_MAX_Y = 5.0;
